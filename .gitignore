node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo

# Never commit API keys
.env
.env.*

# Training data + large model assets (downloaded/generated, not committed)
data/
*.onnx
features.npy

# Bone-suppression checkpoint (downloaded from danielnflam upstream — reproducible, ~13MB; see
# training/bonesupp_weights/README.md). Not committed; the probe script documents how to fetch it.
training/bonesupp_weights/*.tar

# Python bytecode cache
__pycache__/


data/extract_progress.txt
